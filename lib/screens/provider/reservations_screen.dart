import 'package:flutter/material.dart';

class ProviderReservationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pantalla de Reservas (Proveedor)'),
    );
  }
}
